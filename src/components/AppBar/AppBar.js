import AuthNav from '../AuthNav';
import UserMenu from '../UserMenu';

const AppBar = () => {
  return (
    <header>
      <AuthNav />
      <UserMenu />
    </header>
  );
};

export default AppBar;
